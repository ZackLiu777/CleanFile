# Premium 订阅页接入状态

设置 → CleanFile Premium 打开独立订阅页。介绍现有媒体整理、授权文件清理、媒体压缩能力；使用现有主题、卡片与七种语言。当前不改变任何功能的使用权限。

## 尚未开放真实购买

`CleanMyIPhone/Features/Premium/PremiumConfiguration.swift` 的 `purchasesEnabled` 保持 false。当前显示明确的订阅未开放提示，不展示虚构套餐或价格。

配置完整并完成权益交付后，页面使用原生 `SubscriptionStoreView` 展示同一订阅组中的方案，采用 prominentPicker。由 StoreKit 加载本地化价格、周期、可用试用优惠及处理购买和恢复入口，不以安装日期计算试用。

## 开放前必须完成

1. 确认 Premium 权益与免费功能边界，避免先收费而没有权益交付。
2. 在 App Store Connect 创建自动续期订阅组、产品及本地化信息，为相应产品配置免费试用介绍优惠；确认实际月度／年度等方案与时长。
3. 填写配置中的 subscriptionGroupID（不是产品 ID）、privacyPolicyURL、termsOfServiceURL，必须使用真实有效的法律文档链接。
4. 实现经过验证的 StoreKit 当前权益、交易监听与完成交易，以及过期、退款、撤销、恢复购买和待批准状态处理；在明确约定的功能入口接入权限判断。当前页面本身不负责这些业务逻辑。
5. 配置 StoreKit 测试及沙盒环境，验证首次订阅、无试用资格、取消购买、购买失败、续订、恢复和跨设备权益。按项目规则，本地测试执行需用户另行授权。
6. 确认权益、方案及法律说明无误后，才启用 purchasesEnabled。

价格、试用时长和资格不写死到营销文案。免费试用不是所有用户都享有的优惠，不应仅凭本地标记判断。订阅页也不承诺尚未定义的“无限额度”。

## 验收

- 设置页入口可打开和关闭订阅页，未购买不阻塞返回。
- 检查小屏、大字体、VoiceOver、浅色／深色／自定义主题与七种语言。
- 当前未配置状态不得触发支付；正式配置后验证原生方案选择、价格和试用说明、恢复入口及法律链接。
- Build 只能证明编译，真机视觉效果和实际支付流程需要单独验收。
