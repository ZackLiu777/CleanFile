# 消融实验记录

## 2026-09-03：首轮转换实验与全模块候选清单

基线提交：`7011efe5146c6db71bc39f47aa005b2067ae4561`。
本轮仅新增实验测试，不删除功能、不改变生产默认配置。用户已授权本地测试。
遵循 `CHANGE_EVALUATION_RULES.md`：消融是验证必要性，不是预设删除结论。

### 环境与方法

- macOS 26.6.2，Xcode 26.6（17F113），iPhone 17 Pro Simulator / iOS 26.5，Debug，无覆盖率，串行测试。
- 使用真实 `ConversionWorkspace.stage` 和 `ImageBatchConverter`；不复制一套简化业务算法。
- 每组 1 次预热、5 次计量，交错执行不同配置；计时不包括样本生成、校验和清理。
- 导入输入为 1 / 32 / 128 MiB 本地合成文件，输出逐一流式 SHA256 验证，源文件也验证未变。
- 图片输入为固定 2048×1536 PNG，8 项 JPEG 批次，质量 0.82、移除元数据。检查结果数量、格式、尺寸、失败/取消状态；复测 C 还检查各配置输出 SHA256 相同。
- 临时目录隔离，清理仅针对本测试生成的数据。未触碰用户媒体。

测试文件：`Packages/ImageFormatConversionKit/Tests/ImageFormatConversionKitTests/ConversionAblationTests.swift`。

复现（在包目录中运行；结果包路径必须不存在）：

```sh
xcodebuild test -scheme ImageFormatConversionKit \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=latest' \
  -only-testing:ImageFormatConversionKitTests/ConversionAblationTests \
  -parallel-testing-enabled NO -enableCodeCoverage NO \
  -resultBundlePath /private/tmp/CleanFile-Ablation-new.xcresult \
  CODE_SIGNING_ALLOWED=NO
```

### A：消融导入进度的主线程更新

基线：每个复制块回调都更新 MainActor 计数器。
实验：首尾保留，中间最多每 50 ms 更新一次。
诊断组：不更新 MainActor，但仍执行生产复制、回调、同步落盘等链路；**不允许作为产品方案**。

以下为中位数 ms；下降率 = `(基线 - 实验) / 基线`。负数意味着更慢。

| 输入 / 轮次 | 每块更新 | 50 ms 合并 | 下降率 | 无主线程更新（诊断） |
|---|---:|---:|---:|---:|
| 32 MiB / B | 41.522 | 28.750 | 30.76% | 24.237 |
| 128 MiB / B | 88.807 | 55.596 | 37.40% | 80.315 |
| 1 MiB / C | 19.881 | 2.454 | 87.66% | 5.017 |
| 32 MiB / C | 19.686 | 18.061 | 8.25% | 26.295 |
| 128 MiB / C | 58.024 | 67.485 | -16.30% | 42.908 |

C 轮 min–max（ms）：

| 输入 | 每块更新 | 50 ms 合并 | 无主线程更新 |
|---|---:|---:|---:|
| 1 MiB | 3.652–35.418 | 1.824–8.442 | 1.788–6.275 |
| 32 MiB | 15.592–53.538 | 14.968–37.178 | 18.377–37.412 |
| 128 MiB | 41.062–127.176 | 38.121–99.998 | 38.408–125.199 |

结论：**证据不稳定，保留生产实现**。128 MiB 的效果跨轮反向；不能宣称整体导入提升 37% 或 88%。
MainActor sink 只是计数器，不包含实际 SwiftUI 状态发布/渲染。磁盘缓存、调度和模拟器噪声明显，样本数不足以给出可靠的尾延迟或显著性结论。

### B：消融图片批次的一个并发工作位

| 轮次 | 并发 2 中位数 ms | 并发 1 中位数 ms | 并发 1 耗时增加 |
|---|---:|---:|---:|
| B | 97.421 | 179.956 | 84.72% |
| C | 104.103 | 181.110 | 73.97% |

C 轮 min–max：并发 2 为 97.178–116.819 ms，并发 1 为 171.430–239.929 ms。

结论：**拒绝降为单并发，保留默认 2**。此固定输入下并发存在实际收益。没有测峰值内存、能耗、温度，不能据此断言并发 2 在所有设备/大图下最优。

### 验证与证据

- B：2/2 Swift Testing 测试通过，测试执行 9.694 s。
- C（最终测试代码）：2/2 通过，测试执行 9.053 s；编译通过。
- 每轮包括 54 次导入、12 个图片批次；计量部分分别为 45 次导入、10 个批次。
- B 结果包：`/private/tmp/CleanFile-Ablation-20260903-B.xcresult`。
- C 结果包：`/private/tmp/CleanFile-Ablation-20260903-C.xcresult`。
- C 原始日志：`/private/tmp/CleanFile-Ablation-20260903-C.log`，含 `ABLATION_SAMPLE` 和 `ABLATION_RESULT`。
- 临时证据可能被系统清理；本文件保留汇总。未运行完整 App/UI 回归，GitHub Actions 待验证。
- 无性能硬阈值；正确性断言不能因为耗时改善而放宽。

## 后续全模块消融矩阵（尚未执行）

| 候选 | 单一变量 | 指标与保护条件 | 当前决策 |
|---|---|---|---|
| 转换首页恢复任务 | 三个 VM 立即恢复 vs 按需恢复 | 首屏、首次进入工具、恢复完整性、导入竞态 | 先记录实际重复读取量，不直接删恢复 |
| 图片不透明输入 flatten | 保留绘制 vs 条件跳过 | 色彩、方向、alpha、输出质量、时间、内存 | 不能只按格式判断可删 |
| 媒体隐藏页刷新 | 当前刷新 vs 可见时刷新 | 切页延迟、删除后统计、权限变化、增量正确性 | 先建立调用次数基线 |
| 存储快照/索引缓存 | 当前缓存 vs 诊断性旁路 | 冷/热启动、45k+ 数据、总大小一致性、内存 | 不删除已有效的缓存 |
| 旭日图与卡片动画 | 现有开关开/关 | 相同交互脚本的帧时间、CPU、温度 | 必须实机对照；不先改视觉默认值 |
| Liquid Glass | 开/关 | 帧耗时、可读性、控件可用性 | 属产品设计，单凭 CPU 不能决定删除 |
| 恢复后的持久化 | 当前写回 vs 无变化不写 | 写入次数、重启恢复、异常中断与失败状态 | 先确认哪些写回真正冗余 |

这些是候选假设，不是已确认瓶颈。下一轮优先实机验证导入进度和首页恢复；不要同时移除缓存、动画、恢复和持久化，否则无法归因。

安全边界：权限检查、取消、删除确认、部分失败处理、文件完整性、同步落盘、必要的恢复数据和测试断言不作为可随意删除项。
