# 核心数据结构、算法与性能瓶颈

> 最后更新：2026-09-02  
> 本文记录当前仓库中已经实现的数据结构、算法复杂度、实测性能和已确认瓶颈。讨论中的设想不会写成已完成能力。

## 1. 复杂度符号

- `n`：本次处理的文件或媒体资产数量。
- `d`：文件相对路径的平均深度。
- `k`：需要保留的最大文件数量，目前为 10。
- `c`：固定的媒体或文件分类数量。
- `m`：相似照片时间窗口内的候选数量。

在正常目录中 `d` 和 `c` 较小，但面对极深目录时不能把 `d` 永久视为常数。

## 2. 存储扫描

### 2.1 核心数据结构

| 用途 | 当前结构 | 说明 |
| --- | --- | --- |
| 递归枚举 | `FileManager.DirectoryEnumerator` | 由系统和 File Provider 驱动，逐项产生 URL。 |
| 扫描结果 | `[ScannedFile]` | 保存文件 URL、相对路径、分类、真实大小和日期。 |
| 分类统计 | `[FileCategory: CategoryValues]` | 固定分类的哈希表累计器。 |
| 最大文件 | 固定容量最小堆 `[ScannedFile]` | 扫描期间只保留最大的 10 个文件。 |
| 文件夹地图 | 字典树 | 每个目录节点使用 `[String: MutableNode]` 保存子节点。 |
| 删除选择 | `Set<URL>` | 平均 O(1) 判断文件是否被选中。 |
| 扫描事件 | `AsyncThrowingStream<FileScanEvent, Error>` | 流式发送节流后的进度和最终结果。 |
| 持久化索引 | 紧凑单文件 schema v3 JSON | 使用紧凑字段键；兼容读取旧 schema v2 快照。 |
| 快速仪表盘 | `StorageDashboardSnapshot` | 与全量索引分开保存，使存储页先恢复摘要。 |

### 2.2 核心算法

#### 元数据扫描

扫描只预取：

- `isDirectoryKey`
- `fileSizeKey`

文件类型由扩展名分类。扫描不读取文件正文，也不在递归过程中请求 `typeIdentifierKey`，避免 File Provider 为类型信息执行额外元数据往返。

复杂度：

```text
时间：O(n × d)
应用持有空间：O(n × d)
```

系统枚举本身接近 O(n)，但每个文件还要构造和验证相对路径，并更新目录树。完整文件数组和完整树都会随文件数量增长。

#### 相对路径验证

标准化文件 URL 后比较根目录路径组件，拒绝根目录自身、兄弟目录前缀和 `..` 路径穿越。

```text
单文件时间：O(d)
临时空间：O(d)
```

#### 分类汇总

每个文件只更新总计和对应分类桶。

```text
时间：O(n)
空间：O(c)，当前可视为 O(1)
```

#### 最大 10 个文件

使用固定容量最小堆，而不是扫描结束后对所有文件排序。

```text
时间：O(n log k)
空间：O(k)
```

因为 `k = 10`，实际可近似看作 O(n) 时间、O(1) 额外空间。

#### 文件夹树

每个文件沿相对路径逐层更新字典树节点和累计大小，最后递归转换为不可变 `FileNode`。

```text
构建时间：O(n × d)
空间：O(节点总数)，最坏为 O(n × d)
```

#### 删除后重建

删除成功后在后台完成：过滤已删除 URL、重算汇总、重新选择最大文件并重建目录树。准备完成后才一次性发布到 MainActor。

```text
时间：O(n log n + n × d)
空间：O(n × d)
主线程高成本工作：已移除
```

这里仍使用全量排序产生删除后的最大 10 个文件。实测表明，在 20,000 条数据上，强行把统计、树和堆交错为单遍反而更慢，因此当前保留缓存局部性更好的连续数组处理。

#### 快照恢复

JSON 解码后，在后台单次遍历恢复文件数组、摘要、目录树和 Top-10。

```text
时间：O(n × d)
空间：O(n × d)
```

快照准备和恢复都支持协作取消，并且不在 MainActor 上执行。

## 3. 媒体分析

### 3.1 核心数据结构

| 用途 | 当前结构 |
| --- | --- |
| PhotoKit 资产集合 | `PHFetchResult<PHAsset>` 与 `[PHAsset]` |
| 标识符索引 | `[String: PHAsset]` |
| 分类结果 | 分类数组与 `Set<VideoCategory>` |
| 多选状态 | `Set<String>` |
| 真实大小缓存 | `[String: MediaSizeIndexEntry]` |
| Vision 特征缓存 | Actor 隔离的标识符到 Feature Print 索引 |

### 3.2 相似照片算法

当前管线：

1. 按拍摄时间排序。
2. 以 12 秒时间窗口生成候选序列。
3. 请求 256×256 快速缩略图。
4. 使用 64 位差异哈希进行低成本预筛选。
5. 对剩余候选使用 Vision Feature Print 距离。
6. 每张照片最多与窗口内后续 10 张比较。
7. 使用保守聚类，避免传递关系导致误合并。

如果每张照片的比较上限保持为常量，候选比较接近：

```text
排序：O(n log n)
候选比较：O(n)
资产与特征缓存：O(n)
```

如果未来移除比较上限，密集时间窗口最坏会退化为 O(m²)，因此该上限属于性能和误判控制的一部分。

### 3.3 媒体真实大小

真实大小不能从像素或时长可靠推导。当前通过 PhotoKit 资源读取真实字节数，并使用资产标识符与修改日期缓存结果。

主要成本来自：

- iCloud 资源可能尚未本地下载。
- PhotoKit / File Provider 的资源访问延迟。
- 大量资产首次查询时的系统调度和 I/O。

因此采用缓存、去重队列和有界工作，而不是为全部资产同时创建无界 Task。

## 4. 转换模块

### 4.1 核心结构

- Actor 隔离的图片、视频和音频转换 Engine。
- `ConversionImportScheduler` 控制重型导入并发。
- `Set<URL>` 保留已占用输出路径，避免并发文件名冲突。
- 工作区 Manifest 保存导入与输出状态。
- ViewModel 只负责 UI 状态和任务协调。

### 4.2 算法和内存策略

- 图片优先使用 ImageIO，并通过 `kCGImageSourceShouldCache: false` 避免无意义缓存原图。
- 视频和音频优先使用 AVFoundation 原生导出能力。
- 批处理隔离单文件失败并支持取消。
- 不把任意大型文件默认通过 `Data(contentsOf:)` 整体载入内存。

转换复杂度主要取决于输入像素数、帧数、时长和编码器，不能仅用文件数量 `n` 表示。通常近似与需要解码和重新编码的数据量线性相关。

## 5. 已确认的性能数据

### 5.1 实机 Instruments：超过 45,000 个文件

| 区间 | 实测耗时 |
| --- | ---: |
| `Storage Directory Enumerate` | 14.92 s |
| `Storage Scan Finalize` | 26.25 ms |
| `Storage Snapshot Load` | 0.054 ms |
| `Storage Dashboard Load` | 0.093 ms |
| `Storage Sunburst Adapt` | 3.52 ms |

结论：首次扫描约 99.8% 的可见耗时位于系统目录枚举和元数据访问，而不是扫描后的统计、排序或 Sunburst 转换。

### 5.2 Simulator 可重复基准

20,000 个真实小文件：

```text
旧基准：约 1918 ms
当前多轮中位数：约 999 ms
耗时降低：约 47.9%
吞吐提升：约 92%
```

100 万条流式摘要与 Top-10 聚合：

```text
约 4.2–4.4 秒
约 22.8–23.7 万条/秒
```

20,000 条删除后重建对照：

```text
旧前台路径总 CPU：95.18 ms
当前后台准备总 CPU：92.35 ms
总 CPU 改善：约 3%
主要收益：高成本工作不再阻塞 MainActor
```

20,000 条文件详情、50 次选择交互：

```text
旧实现（每次 View 求值重新排序）：6066.12 ms
当前实现（文件版本变化时后台排序一次）：124.20 ms
耗时降低：98.0%
```

20,000 条文件详情、500 次已选容量更新：

```text
旧实现（每次选择遍历全部文件）：3733.21 ms
当前实现（按选择变化增量加减）：0.24 ms
耗时降低：约 99.99%
```

两项实验均验证最终选择集合和已选字节数完全一致。首次排序仍为 O(n log n)，但已移出 MainActor；单次选择由重复排序和全量求和收敛为平均 O(1) 的 Set 更新与容量加减。

100,000 条持久化记录，schema v2 与 v3 同输入对照：

```text
快照体积：14.50 MB → 9.00 MB，降低 37.9%
JSON 解码：357.57 ms → 264.99 ms，降低 25.9%
JSON 编码：286.26 ms → 292.03 ms，增加 2.0%
完整状态恢复：约 900 ms，在后台执行
```

schema v3 缩短了每条文件记录重复出现的字段名。编码存在过 2.0% 的轻微退化，但磁盘占用、读取 I/O 和解码成本明显下降，因此保留；旧 schema v2 快照仍可解码。

100,000 条分块与单体紧凑 JSON 合成实验：

```text
单体临时 Data：约 9.00 MB
最大分块 Data：约 0.90 MB
最大编码缓冲降低：90.0%
总落盘字节：基本不变
编码变化：约 +0.1% 至 +1.4%
解码变化：约 +1.3% 至 -6.0%，属于当前测量噪声范围
```

这项实验只证明分块能降低临时编码缓冲，不能证明实机端到端性能更好。接入生产后，实机出现扫描体感变慢和设备升温，说明额外的分块映射、重复编码与多次原子写入扩大了持续 CPU/I/O 压力。因此生产实现已撤回，继续使用 schema v3 紧凑单文件快照；实验保留作为反例和后续设计依据。

基准测试只用于趋势和回归观察，不设置依赖设备速度的脆弱绝对时间断言。

## 6. 当前最主要瓶颈

### 6.1 第一瓶颈：File Provider 和文件系统元数据枚举

这是当前实机上最大的时间瓶颈。`NSFileCoordinator`、Security-Scoped URL、iCloud 和第三方 File Provider 都可能增加每项元数据访问延迟。

应用可以减少请求的元数据键、避免正文读取和重复扫描，但不能用更多线程绕过 Provider 的串行化、下载或权限协调。无界并发反而可能增加随机 I/O、内存和系统争用。

### 6.2 第二瓶颈：全量 O(n) 常驻模型

当前同时可能持有：

1. `[ScannedFile]` 全量数组。
2. 包含文件叶节点的完整目录树。
3. `[PersistedScannedFile]` 快照准备数组。
4. JSON 编码期间的 `Data`。

在 45,000 文件上仍可工作，但在 100 万文件上会形成明显峰值内存和长时间 JSON 编解码。这是当前距离大规模稳定性目标最大的应用侧瓶颈。

### 6.3 第三瓶颈：单体 JSON 索引

当前文件状态使用 schema v3 紧凑单文件 JSON。快照准备已移出 MainActor 并使用 `.utility` 优先级，但保存和恢复仍需要一次性编码、读取并解码整个索引；目录越大，CPU、内存峰值与磁盘 I/O 越明显。

继续向百万级扩展时，应评估：

- 不延长扫描高负载窗口的增量替换。
- 可暂停或调度到设备空闲期的流式恢复。
- 增量更新。
- 可校验的 schema 迁移。

不能只为了性能直接跳过文件或信任不可靠的目录修改日期，否则会牺牲正确性。

### 6.4 第四瓶颈：首次媒体真实大小查询

真实媒体资源大小受 PhotoKit、iCloud 状态和资源数量影响。缓存能显著改善第二次加载，但第一次查询仍受系统资源可用性限制。

## 7. 下一阶段优化优先级

1. 使用 Instruments 验证 `Storage Snapshot Prepare`、`Storage Snapshot Save` 和 `Storage Deletion Rebuild` 的实机耗时及主线程占用。
2. 量化扫描期间的峰值内存，而不只看 CPU 时间。
3. 重新设计可取消、可迁移的持久化索引；任何分块方案必须先通过实机速度、温度和能耗保护指标。
4. 让目录树和详细文件列表按需恢复，减少首屏同时构造多个 O(n) 表示。
5. 在不牺牲文件正确性和用户数据安全的前提下研究可靠的增量扫描。

## 8. 不应采用的“优化”

- 为每个文件创建一个 Task。
- 无界并发读取 File Provider 元数据。
- 通过估算替代真实文件大小。
- 为了速度跳过路径边界验证。
- 未经可靠失效判断复用旧目录结果。
- 在 MainActor 上构建全量目录树或持久化快照。
- 仅根据单次 Simulator 数字宣称实机性能提升。

项目性能优化仍遵循：

```text
减少 I/O
→ 避免重复计算
→ 增量与缓存
→ 批处理和分块
→ 最后才调整并发度
```
