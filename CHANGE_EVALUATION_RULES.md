# 改动评估与对比实验规则

> 最后更新：2026-09-02  
> 本文规定项目如何判断一次修改是否真正让 App 变得更好。它适用于功能、正确性、性能、稳定性、UI、可访问性、多语言、隐私和产品价值，不仅适用于算法优化。

## 1. 基本原则

任何重要改动都应尽量回答：

```text
修改前是什么状态？
修改后是什么状态？
变化是否超过测量噪声？
正确性和用户安全是否保持？
收益是否值得新增复杂度？
```

默认流程：

```text
定义问题
→ 建立基线
→ 只改变主要变量
→ 使用相同输入复测
→ 比较正确性、成本和用户结果
→ 保留、撤回或继续验证
```

对比实验不是为了证明预设方案正确，而是为了允许方案被数据否决。

## 2. 最高优先级约束

项目判断顺序保持为：

```text
正确性
>
用户文件安全
>
数据完整性
>
稳定性
>
可维护性
>
性能
>
视觉偏好
```

因此：

- 更快但结果错误：撤回。
- 更美观但降低可读性或可访问性：撤回。
- 操作更少但增加误删风险：撤回。
- 测试更容易通过但降低断言标准：禁止。
- 总 CPU 相同但主线程阻塞显著减少：可以保留。
- 收益小于测量噪声且复杂度明显增加：通常撤回。

## 3. 通用实验记录格式

每项重要改动应记录：

```text
问题：
假设：
基线版本：
实验版本：
固定输入与环境：
主要指标：
保护指标：
修改前：
修改后：
绝对变化：
百分比变化：
正确性结果：
限制与噪声：
结论：保留 / 撤回 / 需要实机或 CI 验证
```

百分比变化计算：

```text
越小越好的指标：
(修改前 - 修改后) / 修改前 × 100%

越大越好的指标：
(修改后 - 修改前) / 修改前 × 100%
```

必须同时保留绝对值。只报告“提升 50%”而不报告从多少变到多少，信息不完整。

## 4. 功能真实性评估

功能真实性优先使用确定性验证，不依赖主观 A/B 偏好。

### 4.1 文件与媒体大小

主要指标：

- 与系统或资源真实字节数的一致率。
- 精确、缓存、计算中和不可用项目数量。
- 永久停留“正在计算”的比例。
- 缓存命中率与正确失效率。
- 汇总中未知项目是否被错误计为 0。

保护指标：

- 不为获取大小无界下载 iCloud 内容。
- 不阻塞主线程。
- 不以估算值冒充真实值。

### 4.2 删除

主要指标：

- 请求数量、成功数量和失败数量是否与底层结果一致。
- 成功删除项是否从界面和缓存移除。
- 失败项是否仍然保留。
- 已确认删除字节数是否只包含真实已知值。
- 部分失败是否可恢复、可解释。

保护指标：

- 不扩大用户选择范围。
- 不自动删除原件。
- 不把“进入最近删除”等同于物理空间立即释放。

### 4.3 转换

主要指标：

- 输出文件可重新读取比例。
- 实际格式与用户选择一致率。
- 图片尺寸、动画帧、透明度和元数据策略一致率。
- 视频、音频轨道与时长一致率。
- 单文件失败是否影响批次其他项目。

保护指标：

- 原件不被隐式删除或覆盖。
- 不支持组合不应显示为可用。
- 输出失败不得留下被误认为成功的残缺文件。

### 4.4 相似照片

必须使用经过人工标注的真实样本集比较：

- Precision：推荐结果中真正相似的比例。
- Recall：真实相似组被发现的比例。
- 误合并组数量。
- 建议保留项正确率。
- 单位资产分析耗时与峰值内存。

删除安全优先于 Recall。不能为了找出更多候选显著增加误判。

## 5. 性能与稳定性评估

### 5.1 性能指标

- 冷启动与热启动耗时。
- 首个可交互时间。
- 首屏数据出现时间。
- 总任务耗时。
- 每秒处理数量。
- MainActor 最长阻塞。
- 峰值内存。
- CPU 时间与能耗。
- I/O 次数和读取字节数。
- 创建 Task 和线程的峰值数量。
- 取消后的停止延迟。

### 5.2 测量要求

- 使用相同输入规模和目录结构。
- 区分 Simulator、实机与 CI 数据。
- 重要结论至少多次测量，优先比较中位数。
- 同时记录冷缓存与热缓存场景。
- Instruments 结果应使用 Points of Interest 切分阶段。
- 不把系统等待线程累计权重误认为实际 CPU 时间。
- 单次测试变化不能直接宣称稳定提升。

### 5.3 压力规模

至少关注：

```text
小规模：100–1,000 项
正常规模：10,000–50,000 项
大规模：100,000 项
极端模型：1,000,000 项
```

无法构造真实百万文件时，可以使用流式合成数据验证算法和内存模型，但不能把合成 CPU 基准冒充真实 File Provider 扫描速度。

## 6. UI 与交互评估

UI 不只通过“是否好看”评估。

### 6.1 可用性指标

- 完成核心任务所需步骤数。
- 完成任务所需时间。
- 首次成功率。
- 误触、返回和取消次数。
- 用户是否能预测删除或转换的后果。
- Loading、Empty、Partial Failure 和 Failure 是否可区分。
- 小屏、动态字体和横竖屏下是否截断或重叠。

### 6.2 视觉保护指标

- 文本对比度。
- 动态字体。
- Reduce Motion。
- VoiceOver 顺序与控件语义。
- 触控区域。
- 浅色、深色和自定义背景下的可读性。
- Liquid Glass 开启和关闭时内容是否仍然清楚。

主观外观可以通过截图对比和真实用户偏好评估，但导航正确性、可读性和可访问性必须通过确定性验收。

### 6.3 动画

动画应比较：

- 是否帮助用户理解状态变化。
- 是否增加掉帧、闪屏或 XCTest 无法进入 idle。
- 关闭动画后功能是否保持完整。
- Reduce Motion 是否得到尊重。

动画更复杂不等于体验更好。

## 7. 多语言评估

“存在 Localizable.strings”不等于完成多语言支持。

主要指标：

- 用户可见字符串本地化覆盖率。
- 每种语言缺失 key 数量。
- 切换语言后无需重启即可更新的页面比例。
- 截图、转换指南和帮助文章的语言一致率。
- 复数、日期、数字和文件大小格式是否符合 Locale。
- 长文本语言下的截断与布局错误数量。
- Accessibility Label 和 Hint 的本地化覆盖率。

保护指标：

- 不使用机器翻译结果冒充已人工校验文案。
- 不通过删除难翻译内容提高覆盖率。
- key 存在但仍显示源语言，应视为失败。

## 8. 可访问性评估

主要指标：

- 核心流程 VoiceOver 可达率。
- 可交互元素 identifier 唯一率。
- 控件 Role、Label、Value 和 Hint 正确率。
- Dynamic Type 支持范围。
- Reduce Motion 和 Increase Contrast 行为。
- UI Test selector 稳定率。

UI Test identifier 应放在真正可交互元素上，不应为了测试方便污染多个子元素或改变产品语义。

## 9. 核心价值清晰度评估

核心价值不能只靠团队主观打分，应通过任务和理解验证。

建议问题：

- 新用户在 10 秒后能否说明 App 是做什么的？
- 用户能否区分媒体、存储和转换各自解决的问题？
- 用户只使用一个功能时能否完成完整任务？
- 用户是否理解 App 不能扫描整个 iPhone？
- 用户能否从分析结果自然进入删除、保留或转换？
- 用户是否知道操作成功后实际发生了什么？

主要指标：

- 首次核心任务完成率。
- 从首页进入有效结果的时间。
- 结果页到实际操作的转化率。
- 因误解平台能力产生的失败或退出比例。
- 三个独立功能的单项满意度。

核心价值实验不能诱导用户执行删除，也不能以提高点击率为由弱化确认步骤。

## 10. 隐私与用户文件安全

这类指标不是可交换优化目标，而是必须满足的门槛：

- 用户文件不上传到第三方。
- 日志不记录文件内容、敏感路径和凭据。
- Security-Scoped URL 不越界。
- 删除范围与用户选择完全一致。
- 转换不覆盖或自动删除原件。
- 缓存失效不会导致操作旧 URL 或旧资产。

任何实验只要违反这些条件，无论其他指标提升多少都必须撤回。

## 11. 测试与 CI 规则

对比实验不改变项目现有测试权限：

- Unit Test 使用 Swift Testing。
- UI Test 使用 XCTest / XCUITest。
- 未经用户明确授权，不在本地执行测试。
- 正式测试结果以 GitHub Actions 为准。
- 不删除失败测试、不降低断言、不修改 CI 逃避失败。
- 性能基准默认输出趋势，不使用依赖具体设备速度的脆弱硬阈值。

正确性断言和性能观测应分开：性能波动不能让正确性测试变得不可靠。

## 12. 保留与撤回标准

### 保留

- 正确性和安全性不下降。
- 主要指标有超过噪声的改善，或明确消除了 MainActor 阻塞。
- 保护指标没有显著恶化。
- 复杂度与收益成比例。
- Build 通过，测试按项目规则验证。

### 撤回

- 结果错误或数据语义变得含糊。
- 用户文件风险增加。
- 主要指标恶化。
- 改善只存在于单次偶然数据。
- 为小收益引入长期维护负担。
- 只改善截图，但真实交互退化。

### 继续验证

- Simulator 改善但缺少实机证据。
- 平均值改善但尾部延迟或峰值内存未知。
- 功能正确但真实用户样本不足。
- 不同 File Provider、语言或设备结果明显分化。

## 13. 已有实验示例

存储删除后重建曾尝试两种看似更高效的方案：

```text
强制单遍交错统计、目录树和 Top-N：慢 24%
频繁取消检查的分阶段版本：慢 57.8%
```

两种方案均被撤回。最终方案保持实测更快的连续数组处理，并将高成本重建移出 MainActor：

```text
旧前台总 CPU：95.18 ms
当前后台准备总 CPU：92.35 ms
总 CPU 改善：约 3%
主要收益：约 95 ms 高成本工作不再同步阻塞 UI
```

该示例说明：

> 更少遍历次数不一定更快；必须测量真实实现，并同时区分总 CPU 与用户可感知主线程阻塞。

文件详情页曾在 SwiftUI `body` 的计算属性中排序全部文件，并在每次选择后遍历全部文件计算已选容量。相同输入的对照结果为：

```text
20,000 文件、50 次选择：6066.12 ms → 124.20 ms，降低 98.0%
20,000 文件、500 次容量更新：3733.21 ms → 0.24 ms，降低约 99.99%
```

优化后只在文件数据版本变化时后台生成排序结果；单项选择通过 Set 和已知字节数增量更新。两组实验的选择集合与字节总数均与旧实现一致，因此保留。

文件快照 schema v3 将每条记录的重复 JSON 字段名改为紧凑键，同时兼容读取 v2。100,000 条同输入结果：

```text
体积：14.50 MB → 9.00 MB，降低 37.9%
解码：357.57 ms → 264.99 ms，降低 25.9%
编码：286.26 ms → 292.03 ms，增加 2.0%
```

该改动没有在所有指标上获胜。由于 2.0% 编码退化远小于磁盘体积和解码收益，编码又在后台执行，且旧格式兼容性测试通过，因此结论为保留，并明确记录负面指标。

在紧凑格式之上继续比较 100,000 条单体与每块 10,000 条的持久化方式：

```text
最大临时编码 Data：9.00 MB → 0.90 MB，降低 90.0%
总落盘字节：基本不变
编码变化：两轮约 +0.1% 至 +1.4%
解码变化：两轮约 +1.3% 至 -6.0%
```

编解码时间没有稳定优势，不能宣称分块更快。尽管合成实验显示临时 Data 峰值下降约 90%，生产接入后的实机验证出现扫描速度明显下滑和设备背板更热，速度与能耗保护指标均退化。依据本文件的决策规则，该方案判定为负向改动并已从生产代码撤回；实验测试保留，防止以后只依据合成内存指标再次得出错误结论。
