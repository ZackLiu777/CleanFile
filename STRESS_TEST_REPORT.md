# CleanMyIPhone 压力测试报告

## 1. 测试概况

- 测试日期：2026-09-04
- 项目：`CleanMyIPhone`
- 构建工具：Xcode 26.6（Build 17F113）
- SDK：iOS 26.5
- 测试设备：iPhone 17 Pro Simulator
- 测试方式：Xcode iOS Test Target
- UI 测试：本轮不在本地执行，交由 Xcode Cloud 验证

本轮重点验证存储扫描、元数据聚合、持久化恢复、删除重建、文件选择、媒体转换、流式导入和转换清单保存读取。测试夹具全部使用临时目录，不访问用户真实照片或文件。

## 2. 测试结果摘要

| 测试范围 | 规模 | 结果 |
| --- | ---: | --- |
| 完整 Unit Test Target | 存储、媒体、转换、主题、帮助等全部 Unit 测试 | 通过 |
| 存储性能基线重复测试 | 3 次迭代，重新启动测试进程 | 通过 |
| 转换性能基线重复测试 | 3 次迭代，重新启动测试进程 | 通过 |
| 大文件 I/O 临时压力测试 | 512 MiB 导入、25,000 条清单 | 通过 |
| 本地 UI 测试 | 按要求跳过 | 未执行 |

所有已执行的非 UI 测试最终均返回 `** TEST SUCCEEDED **`，没有测试断言失败或转换异常。

## 3. 存储扫描与持久化

| 场景 | 测试规模 | 观测耗时 | 验证内容 | 结果 |
| --- | ---: | ---: | --- | --- |
| 真实文件元数据扫描 | 20,000 文件、200 个目录 | 882–953 ms | 文件数量、总字节数、目录树、最大文件列表 | 通过 |
| 元数据聚合 | 1,000,000 条记录 | 3.87–4.23 s | 汇总、分类计数、最大文件保持上限 | 通过 |
| 大型快照编码 | 100,000 条记录 | 260–280 ms | 快照编码完整性 | 通过 |
| 大型快照解码 | 100,000 条记录 | 262–296 ms | 解码后记录一致性 | 通过 |
| 状态恢复 | 100,000 条记录 | 873–954 ms | 文件、目录树、分类汇总恢复 | 通过 |
| 删除后状态重建 | 20,000 文件 | 86–97 ms | 删除结果、汇总、目录树和最大文件列表一致 | 通过 |
| 文件选择排序 | 20,000 文件、50 次交互 | 缓存路径约 100–117 ms | 显示顺序和选中状态一致 | 通过 |
| 选中容量计算 | 20,000 文件、500 次交互 | 缓存路径约 0.22–0.30 ms | 选中容量累计正确 | 通过 |

大型紧凑快照相较旧快照：

- 快照体积减少约 37.9%。
- 解码耗时改善约 19%–24%。
- 分块持久化实验显示峰值缓冲区可减少约 90%，但该实验仅作为对比证据，当前生产实现仍使用紧凑快照。

## 4. 媒体与转换

| 场景 | 测试规模 | 观测耗时 | 验证内容 | 结果 |
| --- | ---: | ---: | --- | --- |
| 图片转换 | 600 万像素 JPEG | 47–75 ms | 输出尺寸、输出文件和转换结果 | 通过 |
| 音频转换 | 30 秒音频 | 260–348 ms | AAC 输出文件和输出字节数 | 通过 |
| 流式导入 | 8 MiB | 5.6–7.2 ms | 暂存文件、字节数和输出路径 | 通过 |
| 流式导入 | 32 MiB | 11.7–17.9 ms | 暂存文件、字节数和输出路径 | 通过 |
| 转换清单保存 | 2,000 条记录 | 6.2–11.7 ms | JSON 清单写入 | 通过 |
| 转换清单读取 | 2,000 条记录 | 21.6–23.0 ms | 清单解码和有效源文件过滤 | 通过 |
| 大文件流式导入 | 512 MiB | 854.77 ms，约 599 MiB/s | 目标文件完整、进度单调递增、514 次进度回调 | 通过 |
| 大型转换清单保存读取 | 25,000 条记录 | 保存 83.35 ms，读取 294.73 ms | 记录数量完整恢复 | 通过 |

## 5. 媒体扫描覆盖范围

已通过的 Unit 测试覆盖：

- 媒体分类和视频子类型组合。
- 日期分组和排序。
- 重复媒体结果处理。
- 选择、滚动手势和删除后集合更新。
- 媒体容量统计边界。

本轮没有对真实 Photos Library 执行设备级扫描，因为这需要真实或注入的 PhotoKit 媒体数据、权限状态和真机/专用模拟器环境。上述结果不能替代真实照片库权限和大量媒体资源下的端到端验证。

## 6. UI 与 Cloud 状态

本轮按要求没有继续本地 UI 测试。当前 Xcode Cloud 唯一失败为：

```text
ConversionImportProgressUITests.testProgressViewRefreshesFromZeroToOneHundredPercent()
```

失败点是点击 `conversion.import.simulateHalf` 后，`conversion.import.percentage` 没有在等待时间内变为 `50%`。该问题属于动画文字和 Accessibility 状态刷新时序，不属于本轮非 UI 压力测试的失败。

## 7. 临时文件与代码清理

- 512 MiB 大文件测试源文件已在测试后删除。
- 测试生成的 DerivedData、测试日志和结果包已删除。
- 未访问或修改用户真实照片、视频和文件。
- 未修改生产源码。
- 当前工作区仅保留 Xcode 自动更新的 `UserInterfaceState.xcuserstate`，没有遗留压力测试源代码。

## 8. 结论与后续建议

在 iPhone 17 Pro Simulator、iOS 26.5 环境下，存储扫描、存储状态恢复、删除重建、媒体模型处理、图片/音频转换、流式导入和转换清单持久化均通过了重复压力验证。512 MiB 文件导入过程中未发现数据不完整、进度倒退或临时目录清理异常。

仍建议在发布前补充：

1. 真机 Photos Library 权限下的大量媒体扫描。
2. 1 GiB 以上文件和 iCloud/File Provider 文件的导入测试。
3. 真实大视频编码、取消、磁盘空间不足和导出文件冲突测试。
4. 修复并重新交由 Xcode Cloud 验证导入进度 UI 测试。
